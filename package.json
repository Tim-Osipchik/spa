{
  "name": "spotify",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "live-server public --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
